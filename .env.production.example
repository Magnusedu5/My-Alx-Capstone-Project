# Example production environment variables for Render or docker-compose
# Copy this to .env.production and fill with real values (DO NOT commit .env.production)

# Django
SECRET_KEY=replace-me-with-a-secure-random-string
DEBUG=false
ALLOWED_HOSTS=your-service.onrender.com

# Database (Render will provide DATABASE_URL if you add a managed Postgres)
# Example: postgres://user:password@hostname:5432/dbname
# DATABASE_URL=

# Optional: make entrypoint wait briefly for DB before running migrations
WAIT_FOR_DB=1

# Optional: auto-create admin user at first start (remove these after initial deploy)
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=strongpassword123

# Media: if using S3, set these; otherwise attach a persistent disk and set MEDIA_ROOT
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_REGION_NAME=
